<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sleepy.erik.diplom">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".applicationclass.UsersApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ЕЭТК">
        <activity android:name=".documentsscreens.DocInfoActivity"></activity>
        <activity android:name=".documentsscreens.DocumentsActivity" />
        <activity android:name=".startactivities.RegActivity" />
        <activity android:name=".startactivities.LoginActivity" />
        <activity
            android:name=".mainscreen.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.ЕЭТК.NoActionBar" />
        <activity android:name=".startactivities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>